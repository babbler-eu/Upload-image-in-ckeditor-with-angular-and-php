# Upload-image-in-ckeditor-with-angular-and-php
you have to create a directory inside your localhost "/server" name and just an example you can use those as you like, and you have to copy the file upload_image.php. Inside the server directory you have to create another one called "/image"
